# arduino-test

Arduino Changing Test
New Changing done
more changes
antoher change
dfdf

git init
git [clone url]
cd [foldername]
ls
git add [filename]
git commit -m "message"
git push origin main

for all files
git add .
